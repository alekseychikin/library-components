@import url('../variables.pcss');

.table {
	width: 100%;
}

.table th {
	font-weight: bold;
	text-align: left;
}

.table th,
.table td {
	padding: 0.6rem 0;
}

.table tr:nth-child(even) > * {
	background: var(--light-gray);
}
