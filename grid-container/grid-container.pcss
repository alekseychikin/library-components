@import url('../variables.pcss');

.grid-container {
	display: flex;
	flex-wrap: wrap;
	margin: 0 auto;
	width: min(100%, 102.4rem);
	justify-content: center;
}

.grid-container--left {
	justify-content: flex-start;
}

@media (--desktop) {
	.grid-container {
		flex-wrap: nowrap;
	}
}
